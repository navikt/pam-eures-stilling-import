server:
  port: 9026
spring:
  main:
    banner-mode: off
management:
  endpoint:
    prometheus:
      enabled: true
    metrics:
      enabled: true
  endpoints:
    web:
      exposure:
        include: ["prometheus", "metrics"]

spring.profiles.active: test

shouldUseProxy: true
ProxyUrlHostName: webproxy-utvikler.nav.no
ProxyUrlPort: 8088
ProxyUrlScheme: HTTP


